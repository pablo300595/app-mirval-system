export enum ModalTypes {
    ErrorType = 'error',
    SuccessType = 'success'
}

export enum ModalStatus {
    EnterStatus = 'enter',
    LeaveStatus = 'leave'
}

export enum ModalMessage {
    EmptyMessage = '',
    InvalidFormMessage = 'Please fill in all required fields correctly'
}

export enum ModalEvents {
    CloseModal = 'closeModal'
}